(executable
 (name smoke)
 (libraries soundcheck_core))

(executable
 (name mcp_smoke)
 (libraries soundcheck_core soundcheck_mcp))

; Differential test: the concrete matcher vs the SMT translation. Shells out to
; z3, so it is a (test) — `dune test` runs it alongside the corpus gate.
(test
 (name regex_agree)
 (libraries soundcheck_core soundcheck_kong unix))

; Unit tests for CIDR parsing/membership — pure logic, gated by `dune test`.
(test
 (name cidr_t)
 (libraries soundcheck_core))

(test
 (name vacuous_t)
 (libraries soundcheck_core soundcheck_kong))

(test
 (name contract_t)
 (libraries soundcheck_core))

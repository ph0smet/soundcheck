# Anonymous GET /admin traffic is rejected by key-auth, while authenticated
# traffic remains routable: both halves of the frozen contract hold.
services:
  - name: admin-api
    routes:
      - name: admin-get
        paths: [/admin]
        methods: [GET]
        plugins: [{name: key-auth}]
